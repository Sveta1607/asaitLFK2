# requirements.txt — зависимости бэкенда
# Этот блок создаётся, чтобы запустить FastAPI и ASGI-сервер.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Этот блок создаётся, чтобы загружать переменные окружения из .env.
python-dotenv>=1.0.0

# Этот блок создаётся, чтобы валидировать входные и выходные схемы API.
pydantic>=2.0.0

# Этот блок создаётся, чтобы работать с БД через ORM.
SQLAlchemy>=2.0.0

# psycopg (v3) нужен, чтобы стабильно работать с Postgres на новых версиях Python.
psycopg[binary]>=3.2.0

# Этот блок создаётся, чтобы корректно импортировать jwt в auth_deps.py.
PyJWT>=2.8.0

# Этот блок создаётся, чтобы загружать JWKS по HTTP в auth_deps.py.
requests>=2.31.0

# Этот блок создаётся, чтобы использовать JWK/JWKS утилиты.
jwcrypto>=1.5.6
